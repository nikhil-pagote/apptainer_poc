# slurmdbd.conf — Slurm database daemon configuration
# This file must be 600 and owned by slurm (enforced in docker-entrypoint.sh)
# See: https://slurm.schedmd.com/slurmdbd.conf.html
#
# Note: Slurm 20.11+ dropped PostgreSQL support. MariaDB/MySQL is the only
# supported backend for accounting_storage.

AuthType=auth/munge
DbdHost=slurmdbd
DbdPort=6819
SlurmUser=slurm

LogFile=/var/log/slurm/slurmdbd.log
PidFile=/var/run/slurm/slurmdbd.pid
DebugLevel=info

StorageType=accounting_storage/mysql
StorageHost=mariadb
StoragePort=3306
StorageUser=slurm
StoragePass=slurm_password
StorageLoc=slurm_acct_db
