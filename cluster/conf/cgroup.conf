# cgroup.conf — disable cgroup v2 plugin for container compatibility
# Slurm 23.11 defaults to cgroup/v2 which requires systemd D-Bus.
# Containers lack systemd, so we disable the plugin entirely.
# ProctrackType=proctrack/linuxproc in slurm.conf handles process tracking.
CgroupPlugin=cgroup/v1
